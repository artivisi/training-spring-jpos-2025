<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>dist</id>
    <formats>
        <format>dir</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>
        <!-- Dependencies (includes application JAR) -->
        <fileSet>
            <directory>${project.build.directory}/appassembler/lib</directory>
            <outputDirectory>/lib</outputDirectory>
        </fileSet>

        <!-- Startup scripts from appassembler -->
        <fileSet>
            <directory>${project.build.directory}/appassembler/bin</directory>
            <outputDirectory>/bin</outputDirectory>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Custom scripts from src/dist/bin -->
        <fileSet>
            <directory>src/dist/bin</directory>
            <outputDirectory>/bin</outputDirectory>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Configuration files -->
        <fileSet>
            <directory>src/dist/cfg</directory>
            <outputDirectory>/cfg</outputDirectory>
        </fileSet>

        <!-- Deployment descriptors -->
        <fileSet>
            <directory>src/dist/deploy</directory>
            <outputDirectory>/deploy</outputDirectory>
        </fileSet>

        <!-- Log directory -->
        <fileSet>
            <directory>src/dist/log</directory>
            <outputDirectory>/log</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>
